server.error.include-message=always
server.error.include-binding-errors=always
spring.devtools.restart.log-condition-evaluation-delta=false

database.url=jdbc:mysql://localhost:3306/peerreview
database.username=root
database.password=root

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
# file.upload-dir=G:/My Drive/peer-review
file.upload-dir=C:/Users/Vanya Peshina/Desktop/Final Project - Peer Review/final-project-peer-review-web/src/main/resources/files

#Email setup
spring.mail.host=smtp.gmail.com
spring.mail.port=587
#spring.mail.username=<login user to smtp server>
#spring.mail.password=<login password to smtp server>
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true